const getArticles = require('./articles');
const getMenus = require('./menus');
const getClients = require('./clients');
const getFooterLinks = require('./footerLinks');

module.exports = {
  getArticles,
  getMenus,
  getClients,
  getFooterLinks,
};
